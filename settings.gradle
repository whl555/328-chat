pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "328_Chat"
include ':app'

setBinding(new Binding([gradle: this]))
evaluate(new File(
        settingsDir,
        'test_flutter_module/.android/include_flutter.groovy'
))
